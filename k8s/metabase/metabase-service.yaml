apiVersion: v1
kind: Service
metadata:
  name: metabase
  namespace: default
  labels:
    app: metabase
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: metabase

