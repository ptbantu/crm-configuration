apiVersion: v1
kind: Service
metadata:
  name: activiti-cloud
  namespace: default
  labels:
    app: activiti-cloud
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: activiti-cloud

