apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: default
type: Opaque
stringData:
  # MongoDB Root 用户（管理员权限）
  MONGO_INITDB_ROOT_USERNAME: "bantu_mongo_admin"
  MONGO_INITDB_ROOT_PASSWORD: "bantu_mongo_password_2024"
  # MongoDB 应用数据库和用户
  MONGO_INITDB_DATABASE: "bantu_crm"
  MONGO_INITDB_USERNAME: "bantu_mongo_user"
  MONGO_INITDB_PASSWORD: "bantu_mongo_user_password_2024"

